<Page x:Class="synthesis_program.Views.SaveRecordPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
      mc:Ignorable="d" 
      d:DesignHeight="950" d:DesignWidth="1000">

    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="Auto"/>
        </Grid.ColumnDefinitions>

        <StackPanel Grid.Row="0" Grid.Column="0" Orientation="Horizontal" Margin="10,0,0,15">
            <TextBox x:Name="searchBox" 
                 Width="250" Height="28" 
                 TextWrapping="Wrap"
                 TextChanged="SearchBox_TextChanged"/>
        </StackPanel>

        <StackPanel Grid.Row="0"  
                Orientation="Horizontal" 
                HorizontalAlignment="Center"
                Margin="100,0,10,12">
            <Button x:Name="SearchButton" Width="80" Margin="5" Click="SearchButton_Click"/>
            <Button x:Name="AddButton" Width="80" Margin="5" Click="AddButton_Click"/>
            <Button x:Name="DeleteButton" Width="80" Margin="5" Click="DeleteButton_Click"/>
            <Button x:Name="EditButton" Width="80" Margin="5" Click="EditButton_Click"/>
            <Button x:Name="SaveButton" Width="80" Margin="5" Click="SaveButton_Click"/>
        </StackPanel>

        <!-- 数据表格 -->
        <DataGrid Grid.Row="1" 
                  x:Name="tagsDataGrid"
                  AutoGenerateColumns="False"
                  CanUserAddRows="True" 
                  SelectionMode="Single"
                  SelectionUnit="FullRow"
                  RowHeight="25"
                  FontSize="15"
                  ItemsSource="{Binding TagsList}"
                  SelectedItem="{Binding TagsListSingle}"
                  SelectionChanged="ShowLatestData"
                  Margin="0,0,0,206" Grid.RowSpan="2">
            <DataGrid.Resources>
                <Style TargetType="DataGridColumnHeader">
                    <Setter Property="HorizontalContentAlignment" Value="Center"/>
                    <Setter Property="HorizontalAlignment" Value="Stretch"/>
                    <Setter Property="FontWeight" Value="Bold"/>
                    <Setter Property="Height" Value="30"/>
                </Style>
            </DataGrid.Resources>

            <DataGrid.Columns>
                <DataGridTextColumn Binding="{Binding Id}" IsReadOnly="True" Width="Auto"/>
                <DataGridTextColumn Binding="{Binding MachineKind}" Width="Auto"/>
                <DataGridTextColumn Binding="{Binding BatchNo}" Width="Auto"/>
                <DataGridTextColumn Binding="{Binding BatchCount}" Width="Auto"/>
                <DataGridTextColumn Binding="{Binding Version}" Width="Auto"/>
                <DataGridTextColumn Binding="{Binding MaterialId}" Width="150"/>
                <DataGridTextColumn Binding="{Binding SequenceNoStart}" Width="Auto"/>
                <DataGridTextColumn Binding="{Binding SequenceNoEnd}" Width="Auto"/>
                <DataGridTextColumn Binding="{Binding ModelAddress}" Width="Auto"/>
                <DataGridCheckBoxColumn Binding="{Binding IsCreated}" Width="Auto"/>
                <DataGridTextColumn Binding="{Binding ConnectMachine}" Width="Auto"/>
                <DataGridTextColumn Binding="{Binding Creater}" Width="Auto"/>
                <DataGridTextColumn 
                   Binding="{Binding CreateTime, StringFormat=yyyy-MM-dd HH:mm:ss}" 
                   Width="Auto"/>
                <DataGridTextColumn Binding="{Binding Editor}" Width="Auto"/>
                <DataGridTextColumn 
                                  Binding="{Binding EditTime, StringFormat=yyyy-MM-dd HH:mm:ss}" 
                                  Width="Auto"/>
                <DataGridTextColumn Binding="{Binding Remark}" Width="Auto"/>
            </DataGrid.Columns>

        </DataGrid>

        <DataGrid Grid.Row="2"
          x:Name="tagsLatestData"
          AutoGenerateColumns="False"
          CanUserAddRows="False"
          SelectionMode="Single"
          SelectionUnit="FullRow"
          RowHeight="25"
          FontSize="15"
          ItemsSource="{Binding TagsLatestList}"
          SelectedItem="{Binding TagsLatestListSingle}" Height="158" VerticalAlignment="Bottom">
            <DataGrid.Resources>
                <Style TargetType="DataGridColumnHeader">
                    <Setter Property="HorizontalContentAlignment" Value="Center"/>
                    <Setter Property="HorizontalAlignment" Value="Stretch"/>
                    <Setter Property="FontWeight" Value="Bold"/>
                    <Setter Property="Height" Value="30"/>
                </Style>
            </DataGrid.Resources>

            <DataGrid.Columns>
                <DataGridTextColumn Binding="{Binding Id}" IsReadOnly="True" Width="Auto"/>
                <DataGridTextColumn Binding="{Binding MachineKind}" Width="Auto"/>
                <DataGridTextColumn Binding="{Binding BatchNo}" Width="Auto"/>
                <DataGridTextColumn Binding="{Binding BatchCount}" Width="Auto"/>
                <DataGridTextColumn Binding="{Binding Version}" Width="Auto"/>
                <DataGridTextColumn Binding="{Binding MaterialId}" Width="150"/>
                <DataGridTextColumn Binding="{Binding SequenceNoStart}" Width="Auto"/>
                <DataGridTextColumn Binding="{Binding SequenceNoEnd}" Width="Auto"/>
                <DataGridTextColumn Binding="{Binding ModelAddress}" Width="Auto"/>
                <DataGridCheckBoxColumn Binding="{Binding IsCreated}" Width="Auto"/>
                <DataGridTextColumn Binding="{Binding ConnectMachine}" Width="Auto"/>
                <DataGridTextColumn Binding="{Binding Creater}" Width="Auto"/>
                <DataGridTextColumn 
           Binding="{Binding CreateTime, StringFormat=yyyy-MM-dd HH:mm:ss}" 
           Width="Auto"/>
                <DataGridTextColumn Binding="{Binding Editor}" Width="Auto"/>
                <DataGridTextColumn 
                          Binding="{Binding EditTime, StringFormat=yyyy-MM-dd HH:mm:ss}" 
                          Width="Auto"/>
                <DataGridTextColumn Binding="{Binding Remark}" Width="Auto"/>
            </DataGrid.Columns>

        </DataGrid>
    </Grid>
</Page>