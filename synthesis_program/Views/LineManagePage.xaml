<Page x:Class="synthesis_program.Views.LineManagePage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
      mc:Ignorable="d" 
      d:DesignHeight="800" d:DesignWidth="1500">

    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <Border Grid.Row="0" Background="White" Padding="10" Margin="10,0,0,0"
                    BorderBrush="#FFCCCCCC" BorderThickness="0 0 0 1">
            <StackPanel Grid.Row="0" Grid.Column="0" Orientation="Horizontal" Margin="10,0,0,15">
                <TextBox x:Name="searchBox"         
                     Width="150" Height="28" 
                     TextWrapping="Wrap"
                     TextChanged="SearchBox_TextChanged"/>
            
                <Label Content="机型：" VerticalAlignment="Center" Margin="100,5,0,5"/>
                <StackPanel  Margin="0,5">
                    <ComboBox x:Name="ProdTypecmb" Width="156" Height="26" FontSize="14"
                  BorderBrush="#FFCCCCCC" SelectedIndex="0"
                  IsEditable="True" Loaded="ModelComboBox_Loaded"
                  ItemsSource="{Binding machineKind}"
                  SelectionChanged="ProdTypecmb_SelectionChanged" DropDownOpened="prod_type_DropDownOpened">
                    </ComboBox>
                </StackPanel>
                <Label Content="站点：" VerticalAlignment="Center" Margin="0,5"/>
                <ComboBox x:Name="Station" Width="150" Margin="0,5" SelectionChanged="Station_SelectionChanged"/>
                <Button x:Name="SearchButton" Content="查询" Width="80" Margin="15,5,15,5" Click="SearchButton_Click"/>
                <Button x:Name="DeleteButton" Content="删除" Width="80" Margin="5" Click="DeleteButton_Click"/>
                <Button x:Name="EditButton" Content="修改" Width="80" Margin="5" Click="EditButton_Click"/>
                <Button x:Name="SaveButton" Content="保存" Width="80" Margin="5" Click="SaveButton_Click"/>
            </StackPanel>

            
        </Border>
        <Grid Grid.Row="1" Margin="0,10,0,0">
            <Border Grid.Column="1" Margin="10,0,0,81" Background="White"
                        BorderBrush="#FFCCCCCC" BorderThickness="1">
                <ScrollViewer VerticalScrollBarVisibility="Auto" 
                                  HorizontalScrollBarVisibility="Auto"
                                  Padding="0">
                    <!-- 数据表格 -->
                    <DataGrid Grid.Row="1" 
                      x:Name="linesDataGrid"
                      AutoGenerateColumns="False"
                      CanUserAddRows="True" 
                      SelectionMode="Single"
                      SelectionUnit="FullRow"
                      RowHeight="25"
                      FontSize="15"
                      ItemsSource="{Binding LineMaterialList}"
                      SelectedItem="{Binding LineSelectedSingle}"
                      Margin="0,0,0,206" Grid.RowSpan="2" Height="700">
                        <DataGrid.Resources>
                            <Style TargetType="DataGridColumnHeader">
                                <Setter Property="HorizontalContentAlignment" Value="Center"/>
                                <Setter Property="HorizontalAlignment" Value="Stretch"/>
                                <Setter Property="FontWeight" Value="Bold"/>
                                <Setter Property="Height" Value="30"/>
                            </Style>
                        </DataGrid.Resources>

                        <DataGrid.Columns>
                            <DataGridTextColumn Header="ID" Binding="{Binding ID}" IsReadOnly="True" Width="Auto"/>
                            <DataGridTextColumn Header="机型" Binding="{Binding ProdType}" IsReadOnly="True" Width="Auto"/>
                            <DataGridTextColumn Header="站点" Binding="{Binding Station}" IsReadOnly="True" Width="Auto"/>
                            <DataGridTextColumn Header="站点码" Binding="{Binding Station_Code}" IsReadOnly="True" Width="Auto"/>
                            <DataGridTextColumn Header="料号" Binding="{Binding MachineKind}" Width="Auto"/>
                            <DataGridTextColumn Header="名称" Binding="{Binding BatchNo}" Width="Auto"/>
                            <DataGridTextColumn Header="损耗次数" Binding="{Binding BatchCount}" Width="Auto"/>
                            <DataGridTextColumn Header="预计使用次数" Binding="{Binding Version}" Width="Auto"/>
                            <DataGridTextColumn Header="创建人" Binding="{Binding Creater}" IsReadOnly="True" Width="Auto"/>
                            <DataGridTextColumn Header="创建时间"
                       Binding="{Binding CreateTime, StringFormat=yyyy-MM-dd HH:mm:ss}" IsReadOnly="True"
                       Width="Auto"/>
                            <DataGridTextColumn Header="修改人" Binding="{Binding Editor}" IsReadOnly="True" Width="Auto"/>
                            <DataGridTextColumn Header="修改时间"
                                      Binding="{Binding EditTime, StringFormat=yyyy-MM-dd HH:mm:ss}" IsReadOnly="True"
                                      Width="Auto"/>
                            <DataGridTextColumn Header="备注" Binding="{Binding Remark}" Width="Auto"/>
                        </DataGrid.Columns>
                    </DataGrid>
                </ScrollViewer>
            </Border>
        
            <Border Grid.Column="1" Grid.Row="2" Margin="10,0,0,0" VerticalAlignment="Bottom" Background="White" BorderBrush="#FFCCCCCC" BorderThickness="1" Height="62">
                <Label x:Name="lblWarning"  Content="" HorizontalAlignment="Center" VerticalAlignment="Center" Width="200" Height="30" FontWeight="Bold" Foreground="Red"/>
            </Border>
        </Grid>
    </Grid>

</Page>